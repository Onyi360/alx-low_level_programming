#include "main.h"

/**
 * mul - Multiply two integers
 * @x: The number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplication between x parum and y parum
*/

int mul(int x, int y)
{
	return (x * y);
}
