#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 *
 */

int main(void)

{

	char lowr;

	for (lowr = 'z'; lowr >= 'a'; lowr--)
	putchar(lowr);
	putchar('\n');

	return (0);

}
